\documentclass[margin=0mm]{standalone}
\usepackage{tikz}
\usepackage{pgfplots}
 \pgfplotsset{compat=newest}


\usepgfplotslibrary{groupplots}
\usetikzlibrary{math,matrix,fit,positioning}

\usepackage{currfile,hyperxmp}


\begin{document}



  
  

\begin{tikzpicture}[
declare function={ 
	radius(\L) = sqrt( \L * (\L +1) );
	xcomp(\L,\zz) = sqrt( radius(\L)^2 - (\zz)^2 );
	  }
]
%\useasboundingbox (0,0) rectangle (5,5);
%\draw (0,0) rectangle (5,5);
 		
  	 \begin{axis}
            [  width= 45mm,
                axis x line=center,
                axis y line=center,
                ymin=0, ymax=3,
                xmin=0, xmax=3,
                axis equal image,
                 ymajorgrids,
                 ytick distance=1,
    grid style={line width=.2pt,draw=gray!50},
    xlabel = {$x$},
    ylabel = {$z$},
    yticklabels  = {},
    xticklabels  = {},     x label style={anchor=north west},
     y label style={anchor=north east},
            ]
            ]	
            
         %  \tikzmath{ \s = 1; \ms = 0; \l = 2; \ml = 2; \j=3; \mj = 2;}  
        \tikzmath{ \s = 1; \ms = 1; \l = 2; \ml = 2; \j=3; \mj = 3;}  
  
  			\draw[->,thick] (0,0) -- node {$\vec{l}$} ( {xcomp(\l, \ml)} ,\ml);
  			\draw[->,thick]( {xcomp(\l, \ml)} ,\ml)  -- node {$\vec{s}$} ++ ( {xcomp(\s, \ms)} ,\ms);
  	
  			
  			\draw[->,thick] (0,0) -- node {$\vec{j}$} ( {xcomp(\j, \mj)} ,\mj);
                
       \end{axis}  		

  		
\end{tikzpicture}


\end{document}